Improve the overall UI experience across all quiz screens and the 
recommendations screen with the following changes:

1. QUIZ SCREEN BACKGROUNDS
Each quiz question should have a distinct, light illustrated 
background that adds visual warmth without distracting from content.
Implement this using a subtle full-screen background on each question:

Question 1 (Mood): Soft gradient — from #E8F4F0 (top) to #F8F9FA 
  (bottom). Add 3 large soft circles (opacity 0.08, color #0D7377) 
  positioned at: top-right (200px diameter), center-left (150px), 
  bottom-right (120px). These are decorative blobs behind content.

Question 2 (Companion): Gradient from #EEF2FF to #F8F9FA.
  Soft purple blobs — color #7F77DD, opacity 0.07, 
  same positioning pattern.

Question 3 (Duration): Gradient from #FFF8EC to #F8F9FA.
  Soft amber blobs — color #C9962B, opacity 0.08.

Question 4 (Budget): Gradient from #F0FFF4 to #F8F9FA.
  Soft green blobs — color #0D7377, opacity 0.06.

Question 5 (Interests): Gradient from #FFF0F0 to #F8F9FA.
  Soft coral blobs — color #D85A30, opacity 0.07.

Question 6 (Activity): Gradient from #F0F4FF to #F8F9FA.
  Soft blue blobs — color #1A3C5E, opacity 0.08.

Question 7 (City): Gradient from #F5F0FF to #F8F9FA.
  Soft purple blobs — color #7F77DD, opacity 0.07.

Implementation: Use a fullscreen div with position:fixed, z-index:-1, 
containing the gradient background + 3 absolutely positioned 
border-radius:50% divs for the blobs. Switch based on current 
question index.

2. QUIZ OPTION ANIMATIONS
Add these micro-interactions to all quiz option buttons:
- On mount: each option slides up and fades in sequentially 
  (translateY 20px → 0, opacity 0 → 1, duration 300ms, 
  stagger 80ms between each option)
- On tap/select: scale(0.97) for 100ms then bounce back to scale(1.02) 
  then scale(1) — total 250ms
- Selected state transition: border color and background color 
  animate over 200ms ease

3. PROGRESS BAR
Make the progress bar taller (6px instead of 4px) and add a 
subtle glow on the filled portion:
box-shadow: 0 0 8px rgba(13, 115, 119, 0.4)
Animate width change over 400ms cubic-bezier(0.4, 0, 0.2, 1)

4. SCREEN TRANSITION
Add a page-level fade + slight upward slide transition between 
quiz questions:
- Exiting question: opacity 1→0, translateY 0→-10px, 150ms
- Entering question: opacity 0→1, translateY 15px→0, 250ms
- Use React state + CSS transition classes, not a library

5. RECOMMENDATIONS SCREEN
Add entrance animations to destination cards:
- Each card fades in and slides up from 24px below
- Stagger: 120ms delay between each card
- Duration: 350ms, ease-out
- Only animate on first load (use a ref to track if already animated)

6. LANDING SCREEN
Add a soft pulse animation to the 3 gold dots:
scale 1 → 1.3 → 1, duration 1.8s, ease-in-out, infinite,
stagger 300ms between each dot.

Add a slow parallax feel to the gradient background:
background-size: 200% 200%
animation: gradientShift 8s ease infinite
@keyframes gradientShift — shift background-position from 
0% 50% → 100% 50% → 0% 50%